name: Modi_Workflow_1

on: [ push, pull_request ]

jobs:
  myBuild:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Run a one-line script
      run: echo "Hello, Modi World!"
      
    - name: Get directory info and see what it has
      run: ls
